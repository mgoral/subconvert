# subconvert/cli/Makefile.am

cli_PYTHON = \
	__init__.py \
	MainApp.py

#CLEANFILES += $(cli_PYTHON)

clidir = $(datadir)/subconvert/subconvert/cli

CLEANFILES = *.pyc *.pyo

clean-local:
	rm -rf __pycache__
